#include <limits.h>
#include <stdio.h>

int main() {
  int z;   // 32 - 2^32 | 64 2^64
  short x; // 32 bit - (2^16 - 1),
  long y;
  unsigned int w;
  printf("%u", w);
  printf("%hd", x);
  long double zed;

  return 0;
}
